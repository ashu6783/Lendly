# Render Blueprint — LMS (Docker)
# Deploy: Dashboard → New → Blueprint → connect repo → use this file.
# Docs: https://render.com/docs/blueprint-spec

services:
  - type: web
    name: lms-api
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /api/health
    preDeployCommand: node dist/seed.js
    buildFilter:
      paths:
        - backend/**
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: CLIENT_URL
        fromService:
          type: web
          name: lms-web
          envVarKey: RENDER_EXTERNAL_URL
      - key: MAX_FILE_SIZE_MB
        value: 5

  - type: web
    name: lms-web
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./frontend/Dockerfile
    dockerContext: ./frontend
    healthCheckPath: /
    buildFilter:
      paths:
        - frontend/**
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: lms-api
          envVarKey: RENDER_EXTERNAL_URL
